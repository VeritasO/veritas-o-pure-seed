import React from 'react'
const TribunalPanel = ({ tribunalId }) => {
  // Fetch and display tribunal logs, votes, agent comments, etc.
  return <div>Tribunal session for {tribunalId}</div>
}
export default TribunalPanel